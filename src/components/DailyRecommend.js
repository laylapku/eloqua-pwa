import React from "react";
import BottomBar from "./BottomBar.js";

const DailyRecommend = () => {
  return (
    <div>
      <p>Today's recommend:</p>
      <BottomBar />
    </div>
  );
};

export default DailyRecommend;
